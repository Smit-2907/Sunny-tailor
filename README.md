
  # B2B Web App Design System

  This is a code bundle for B2B Web App Design System. The original project is available at https://www.figma.com/design/wxstuxfPY26yS5RkJkI4DB/B2B-Web-App-Design-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Sunny-tailor
